import '../css/calc-screen.css';

const CalcScreen = ({ calc }) => {
    return (
        <div className="calc-screen">
            <div>{calc}</div>
        </div>
    );
};

export default CalcScreen;
